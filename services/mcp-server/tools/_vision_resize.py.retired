"""Compatibility re-export — vision resize lives in ``libs/ocr_core/``.

Kept as a thin re-export so any stale imports of
``services.mcp-server.tools._vision_resize`` still resolve. New code should
import from :mod:`ocr_core` directly.
"""

from __future__ import annotations

from ocr_core import (  # noqa: F401 — re-exports for back-compat
    VisionProfile,
    classify_content,
    profile_for_model,
    resize_to_budget,
)
