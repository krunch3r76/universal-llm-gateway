"""Package-shadow stub for ``tools.agent_bus``.

Implementation lives in the ``agent_bus/`` package; Python resolves
``import tools.agent_bus`` to ``agent_bus/__init__.py``. This file satisfies
descriptor and tooling paths that still reference ``agent_bus.py`` after the
Wave 0 split (pure move, zero behavior change).
"""

from __future__ import annotations
