# Vortex bearer token (standalone mode only — sandbox mode auto-injects auth).
# Set this to your MCP_AUTH_TOKEN value from ~/.gateway/mcp.yaml to pre-fill
# the token field. Alternatively, paste it in the Settings panel at runtime.
VITE_VORTEX_TOKEN=your-vortex-bearer-token-here
