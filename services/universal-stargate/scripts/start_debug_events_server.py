#!/usr/bin/env python3
"""
DEPRECATED: This script is no longer needed.

Debug event broadcasting is now integrated directly into the main application.
Use the main proxy with --debug-events flag instead.

Example:
    python start_proxy.py --debug-events
"""

print(
    "⚠️ This script is deprecated. Debug event broadcasting is now integrated into the"
    "main application."
)
print("   Use: python start_proxy.py --debug-events")
print("   Or: python scripts/start_proxy_with_debug.py")
