"""
Common utility functions shared across streaming and non-streaming pipelines.
"""


# truncate_for_logging() removed - JSON truncation now via format_json_for_log()
# from universal_logging.json_utils (used by both logging and GUI display)
