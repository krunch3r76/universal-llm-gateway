# External dependencies for universal_transport
# Universal LLM Ecosystem components (universal_logging, universal-event-bus, etc.) 
# are NOT in requirements.txt - they are expected in the Python library search path

# Core runtime dependencies
pydantic>=2.0.0
typing-extensions>=4.0.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Linting and formatting (ecosystem standard)
ruff>=0.1.0

# Type checking (ecosystem standard)
basedpyright>=1.0.0
