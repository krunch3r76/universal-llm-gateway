# External dependencies
# Note: Universal LLM Ecosystem components (universal_logging, universal-event-bus,
# universal-transport) are NOT included here. They are expected to be in the Python
# library search path (shared virtual environment).

# YAML configuration support
PyYAML>=6.0

# Resource monitoring
psutil>=5.9.0

# GPU monitoring (optional)
# Uncomment if GPU monitoring is needed
# nvidia-ml-py3>=7.352.0

# Async MQTT support
asyncio-mqtt>=0.16.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Code quality tools (install via IDE extensions or manually)
# ruff>=0.1.0  # Use IDE extension or install separately
# basedpyright>=1.0.0  # Use IDE extension or install separately
