# Configuration Reference

All configuration files, environment variables, and paths.

## Config Files

| File | Purpose | Generated by |
|---|---|---|
| `~/.gateway/stargate.yaml` | Master Stargate config | `./manage` (first start, never overwritten) |
| `~/.gateway/nodes/<host>.env` | Per-node: NODE_ID, MODEL_PATH, federation keys | `./manage` |
| `.env.local` | Local env: MODEL_PATH_ROOT | Manual |
| `~/.gateway/rag.yaml` | RAG service watcher/scope config | Manual (legacy: `~/.rag/config.yaml`) |
| `~/.gateway/mcp.yaml` | MCP server config (auth, data dir, TLS) | `./manage` template, then manual |
| `config/stargate_config.yaml` | Default Stargate config template | Source |

## Environment Variables

### Stargate

| Variable | Purpose | Default |
|---|---|---|
| `STARGATE_CONFIG` | Path to Stargate config YAML | `config/stargate_config.yaml` |
| `STARGATE_MODE` | Override federation mode | From config |
| `STARGATE_UNIX_SOCKET` | Unix socket path (Edge mode) | — |
| `STARGATE_ENABLE_TCP_MONITORING` | Enable TCP monitoring on 9997 | false |
| `FEDERATION_KEY` | Edge federation API key | — |
| `FEDERATION_KEY_EDGE_*` | Per-edge federation keys | — |
| `DATA_DIR` | Request snapshot base directory | — |
| `LOG_DIR` | Log base directory | `/tmp/logs/universal-stargate` |

### Gateway

| Variable | Purpose | Default |
|---|---|---|
| `LOG_DIR` | Log base directory | `/tmp/logs/universal-llm-gateway` |
| `MODEL_PATH` | Model file directory | — |

### RAG

| Variable | Purpose | Default |
|---|---|---|
| `GATEWAY_URL` | Embedding endpoint URL | `http://localhost:9999` |

## Stargate Config Structure (stargate.yaml)

```yaml
federation:
  mode: master                      # master | remote | edge
  stargate_id: master-localhost

  local_edge:
    socket_path: "/tmp/universal-protocol/edge-localhost.sock"
    stargate_id: edge-localhost
    api_key: "${FEDERATION_KEY_EDGE_LOCALHOST}"

  remotes:
    - stargate_id: relay-jupiter
      url: "http://jupiter:9999"
      api_key: "${FEDERATION_KEY_JUPITER}"

routing:
  scoring:
    weights: { resource_fit: 0.4, staleness: 0.3, stability: 0.3 }
  sticky_models: []                 # model IDs that pin to one gateway

request_queue:
  timeout: 120                      # queue_timeout seconds
  upstream_retry_timeout: 90        # retry timeout for upstream errors

token_management:
  enabled: true

pipelines:
  search_paths:
    - pipelines
    - pipelines.local
  output_dir: /tmp/logs/universal-stargate/pipeline_summaries
```

## RAG Config (~/.gateway/rag.yaml)

Scopes and watch directories can be edited in this file by hand, or added at runtime via `POST /scopes` on the RAG service (or `POST /api/v1/rag/scopes` via Stargate). Runtime registration updates the live config and persists to this file so new scopes survive restarts.

`WatchDirectory` fields: `path`, `extensions` (optional; defaults to baseline `[".md", ".txt", ".html", ".htm", ".pdf"]`), `recursive` (default `true`), `chunk_tokens` (optional int — overrides default 512-token chunk size for that directory; use 1024 for ebooks), `exclude` (optional filename globs matched with `fnmatch`; excluded files are skipped by startup, reconcile, and hot-reload indexing).

Top-level field: `embedding_model` (model ID used for all `/v1/embeddings`
requests from RAG; defaults to `bge-m3-q8-0-8192-cpu`).

`extraction` fields: `enabled` (toggle index-time structured extraction), `pipeline`
(virtual pipeline/model ID; default `rag-extraction`), `schema_version`
(integer to force re-extraction by hash invalidation), `property_boost_factor`
(hybrid search distance multiplier for chunks matching extracted properties).

`article_registry_path` (optional): path to a legacy article registry YAML for one-time migration into `~/.rag/store/rag_metadata.db`. When set, the RAG service loads it at startup and merges citation metadata into the `articles` table. After migration, article data lives in the DB; chunks from registered files expose `article_title`, `article_authors`, and `published_date` in search results and rag-context output.

Corpus hints and scope vocabulary are stored in `~/.rag/store/rag_metadata.db` (tables `corpus_hints`, `scope_vocabulary`). Populated by post-index enrichment (`python -m services.rag.corpus_hints`, `scripts/rag/classify_vocabulary.py`). No YAML paths; read path is DB-only.

`post_index_enforcement` (default `strict`): controls behavior when post-index enrichment watermarks are stale relative to the last reindex. `strict` returns 503 on search until watermarks are current; `warn` logs ERROR at startup but continues serving. Post-index steps tracked: `corpus_hints`, `vocabulary`, `bibliography`.

`contextualize_model`: model ID for per-chunk LLM-generated context prefixes prepended before embedding. Omit or set to a model ID to enable (default: `qwen3-embedding-8b`). Set to `""` to disable.

`reconcile_interval_s` (default 300): seconds between watcher reconcile sweeps that recover files missed by inotify. Set to 0 to disable.

```yaml
watch_directories:
  - path: /mnt/torus/projects/universal-llm-gateway/docs/architecture
    extensions: [md]
    recursive: true
  - path: /mnt/torus/projects/universal-llm-gateway/docs/vision
    extensions: [md]
    recursive: true
  - path: /mnt/torus/projects/universal-llm-gateway/docs/engram
    extensions: [md, txt]
    recursive: true
  - path: /mnt/torus/projects/universal-llm-gateway/tasks/journal
    extensions: [md, yaml]
    recursive: true
  - path: /mnt/torus/projects/universal-llm-gateway/docs/reference
    extensions: [pdf, epub]
    chunk_tokens: 1024
    exclude: [CORPUS_MANIFEST.md]
    recursive: true
embedding_model: bge-m3-q8-0-8192-cpu
# Optional: path to legacy article registry YAML for one-time migration into rag_metadata.db
# article_registry_path: /mnt/torus/projects/universal-llm-gateway/docs/research/article_registry.yaml
# Post-index enrichment enforcement: strict (503 until current) | warn (log only)
post_index_enforcement: strict
extraction:
  enabled: true
  pipeline: rag-extraction
  schema_version: 1
  property_boost_factor: 0.5
```

Ebook files (PDF, EPUB) belong in `docs/reference/`. They are indexed at 1024 tokens/chunk and retrieved via `--scope ebooks` in `scripts/consult`. EPUB extraction uses `ebooklib` + `BeautifulSoup`; PDF uses `pymupdf4llm`.

## MCP Config (~/.gateway/mcp.yaml)

Optional. Controls the internet-facing MCP server container. If absent or `auth_token` is empty, the TUI treats MCP as unconfigured and skips it during fleet deploys.

Fields: `auth_token` (bearer token for client auth), `data_dir` (host path mounted as `/data/files`, default `~/mcp-data`), `project_dir` (host path mounted read-only as `/data/project`, defaults to workspace root), `tls_cert_dir` (host path containing `fullchain.pem`/`privkey.pem`, default `/etc/letsencrypt/live/mcp.k-1.me`).

Project list/search MCP tools default to git-tracked files only; **`include_untracked=True`** lists or searches the real tree (e.g. gitignored `tmp/`). See `docs/mcp-integration.md`.

## Log Paths

| Service | Path |
|---|---|
| Stargate (host) | `/tmp/logs/universal-stargate/universal_stargate.log` |
| Gateway (container) | `/golem/logs/gateway/gateway.log` |
| Edge Stargate (container) | `/golem/logs/stargate/universal_stargate.log` |
| Workers (container) | `/golem/logs/workers/` or `/tmp/process_ipc/{model_id}` |
| RAG | `/tmp/logs/universal-rag/rag.log` |

## Event Paths

| Source | Path |
|---|---|
| Host events | Event Service (`scripts/query-events`) |
| Edge Stargate events | Container: `/tmp/stargate-events/current.jsonl` |
| Edge Gateway events | Container: `/tmp/_universal-gateway-events/current.jsonl` |

## Socket Paths

| Socket | Purpose |
|---|---|
| `/tmp/universal-protocol/edge-localhost.sock` | Master ↔ local Edge |
| `/tmp/universal-protocol/*.sock` | IPC sockets |
| `/tmp/process_ipc/*.sock` | Worker process IPC |

## Port Semantics

| Port | Accessible to | Purpose |
|---|---|---|
| 9999 | Clients, agents, curl | Sole external endpoint (Stargate) |
| 9998 | Container-internal only | Gateway (inference) |
| 9997 | Host (optional) | TCP monitoring |
| 8100 | Host | RAG service |
