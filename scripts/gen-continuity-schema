#!/usr/bin/env bash
# CLI entry: scripts/gen-continuity-schema {generate|check}
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
exec "${HOME}/.venvs/universal/bin/python3" -m scripts.gen_continuity_schema "$@"
