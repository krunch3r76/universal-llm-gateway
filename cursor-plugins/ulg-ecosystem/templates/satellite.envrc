# Pin to ULG shared venv — sitecustomize injects universal-llm-gateway/libs into sys.path.
# ∀ satellite: use this venv only (¬ private project venv).
# Requires: direnv allow (once per checkout) if using direnv.
export VIRTUAL_ENV="${HOME}/.venvs/universal"
PATH_add "${VIRTUAL_ENV}/bin"
