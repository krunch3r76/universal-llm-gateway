---
description: Anthropic substrate stub — anthropic/* API prohibited; cursor/* OK except Fable; CDP preferred when packable
alwaysApply: true
rule_category: workflow
apply_tier: catalog
trigger_match_terms:
  - anthropic/
  - Opus
  - Fable
  - team_dispatch
  - cursor/claude-opus
---
# Anthropic substrate (always-on stub)

`∀ Cursor lead dispatch involving Anthropic-family models:`

| Want | Do |
|---|---|
| `anthropic/*` (Stargate API) | **PROHIBITED** by default |
| `cursor/*` (cursor-sdk) | **OK** except **Fable** (`cursor/claude-fable-5` is carded Other Models; **blocked for cost** — use `cdp/fable`) |
| Opus / Sonnet product consult | **web-anthropic-cdp** preferred (cortex-packaged corpus); **encourage Fable** when outside perspective helps |
| Need live checkout browse | **`cursor/claude-opus-*`** — **inform-then-proceed** when escalation warrants (announce); ¬ default wait for approval |

¬ equate “Opus High” with `anthropic/claude-opus-*`. 
¬ use `cost_intent=deliberate_high_cost` as routine license for API.

**SOT:** Use the `consult-routing` skill § Anthropic-family substrate · rule `anthropic-dispatch-authorization_ws.mdc` · `decision:anthropic-family-dispatch-substrate` · Fable/opus inform: `decision:operator-proxy-seat-posture`.
