---
description: Load CDP operator-proxy protocol when Cowork web-anthropic holds the operator seat via agent_bus.request — co-developer posture, DIRECTIVE/CLOSEOUT/DISPOSITION discipline.
alwaysApply: false
rule_category: workflow
apply_tier: catalog
trigger_match_terms:
  - operator-proxy
  - cdp-operator-proxy
  - DIRECTIVE
  - CLOSEOUT
  - DISPOSITION
  - cursor-auto
  - assumed_state
  - operator proxy
  - propagate
  - sync_restart
  - service restart
  - operator-proxy-substrate
---
# CDP Operator Proxy — Stub

**Invariant:** `operator-proxy episode ∧ agent_bus.request from Cowork ⇒ Use the cdp-operator-proxy skill before authoring DIRECTIVE bodies or dispositing CLOSEOUTs.`

**Operator identity (BINDING):** `default(operator) = model_seat` — assume **non-human** unless Kaywan **explicitly declares** human operator (skill invariant 0). Cowork CSE presence alone ≠ human operator.

**Mission default (BINDING):** CDP Opus mission launch (`purpose=operator-proxy|mission` via `team_dispatch(model=cdp/…, purpose=…)` primary; `project_ask(purpose=…)` escape) ⇒ **Opus = operator · Fable = advisor · cursor/grok-4.5 = reasoner · cursor-auto = executor · tick = admitter**. Auto-inject on mission submit. Substrate hypotheses → reasoner (`contract: investigate`, inv 28). Fable escalate: prefer `team_dispatch(model=cdp/fable)`. Tick posting + B1/B2 nest stall: `cortex://notes/system/specs/cursor-auto-tick-work-posting.md`.

**Thread privacy:** operator-proxy DIRECTIVE/CLOSEOUT/DISPOSITION live on a **private `request` thread** — ¬ the IDE orchestrator’s endeavor root (cite root in `arc:` only). Skill invariant 11.

Mechanism (turn shapes, boot checklist, anti-patterns): Use the `cdp-operator-proxy` skill — not this stub.

**Two slugs (BINDING — `decision:operator-proxy-skill-surface-split`):** `cdp-operator-proxy` (`shared_sync`) is the **operator contract** — what the operator seat executes and must expect; it is the slug to cite in operator-facing hints. **Cursor seats** additionally Use the `operator-proxy-substrate` skill (`cursor_only`) for the **mechanism**: admit gates (scope / `vision:` / fix hints), `nest_under` + lease, auth-gate budget enforcement, supersede revert, GIW drain hazard, and CDP packet + chip delivery. Carve rule: `shared_body ⇒ observable behavior` · `code_body ⇒ mechanism`.

**Protocol SOT:** `cortex://notes/system/specs/cdp-operator-proxy-v0.md` (field tables, transport).  
**Doctrine:** `decision:operator-proxy-seat-posture`.  
**Transport sibling:** `claude-ai-cdp-navigation` for CDP/Jupiter mechanics (incl. warm same-CSE follow-up).

**Session abort (BINDING):** IDE/cursor lead **¬** `project_ask(op="abort")`, cancel cursor-sdk dispatches, or kill in-flight executions without **explicit operator authorization** for that session — poll, read closeout, paste status into live CDP converse; operator supersedes on private lane. Rule: `session-abort-authorization_ulg.mdc`.

**Operator restart (BINDING):** `agent_bus.request` with `contract: propagate` — cursor-auto mints propagation rows and coordinates drain-gated `sync_restart` (not tier-M `execute` + `manage.*`). Skill § Tier-M / propagate templates.

**Transport vs bus (BINDING):** `project_ask` = IDE↔Cowork converse transport; operator commission = **`agent_bus.request` on private request thread**. `abort(project_ask)` does **not** cancel bus work — poll the request lane, not `active_work` alone. Reconnect: warm submit same `holder` + `converse=true` (Cowork CSE) **or** continue on bus thread (`mark_read` → next `request`).

## When

- Cowork holds operator seat driving cursor via `agent_bus.request`
- Authoring or reviewing `TYPE: DIRECTIVE` / `TYPE: DISPOSITION` turns
- cursor-auto nested dispatch arcs (5867-class dogfood)
- Blocked on a human/cursor fact — skill § Blocked → ask ladder (observe → cursor → Cowork Ask/push); ¬ silent stop
- Life missing a code-seat tool (`manage`, reload, …) — request **cursor**, ¬ park on Kaywan

¬ load for IDE cursor lead orchestration (use `lean-context-dispatch-first` + `operator-posture` instead).
IDE wakes/corrections to a live CSE: Use the `claude-ai-cdp-navigation` skill § Warm follow-up (CDP into chat; ¬ bus-only + push).
