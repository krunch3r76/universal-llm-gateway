---
description: Dispatch kernel — dispatch-first, Explore-first recon, escalation ladder + binder order, anthropic/* prohibited, non-primary model gate, bind-then-compose split, recon before implement, request front door. Always-on; procedure lives in consult-routing / subagent-strategy / conductor / cdp-operator-proxy.
alwaysApply: true
rule_category: workflow
apply_tier: required_gate
---
# Dispatch kernel

**Invariant:** `lean_context ⇒ dispatch(heavy read | author | implement) off-seat; lead orchestrates`.
Before non-trivial implement or breadth investigation ask "can this be a dispatch?" — default **yes**;
stay in-seat only for trivial/local touches or latency-sensitive tight-loop debugging.

## Read side — Explore-first

`loci_unknown ∨ ≥3_files ∨ 2nd_speculative_grep ⇒ Task(subagent_type="explore")` (parallel when ≥2
independent questions). Explore ≡ Cursor subagent; in-seat Grep/Read is not Explore. Omit `model=`.
Sparse work item (`density_triage` unset or `recon_pending`) ⇒ recon front-half before **implement**
(`require_implement_ready` guards implement, not conductor spawn). Exempt: `mechanical`/`trivial`, or dense
spec with `files_expected` + `acceptance_criteria`. Todo stage attrs are spawn seeds only — scoreboard fold
is current state while G1–G3 open. Bugfix on pattern P ⇒ service sweep (`[quality:bug-class-sweep]`).

## Ladder + binder order

| Need | Path | Autonomy |
|---|---|---|
| Mechanical implement | `team_dispatch(op=generate, seat=cursor-sdk)` → Composer (omit `model=`) | autonomous |
| Investigate residual · conductor drive | Composer (omit `model=`, `contract=conductor\|investigate`) — returns `OPEN FORK:` lines, never binds | autonomous |
| Greater reasoning / independent check | **`cdp/fable`** width · **`cdp/opus-5`** bind/review — announce `CDP: <trigger> — <why>`, then proceed | autonomous on trigger |
| Premium live checkout | `cursor/claude-opus-5` — inform-then-proceed (wait only if premium spend paused) | announce |

Binder order: `cdp/opus-5` → **`cdp/fable`** → `cursor/gpt-5.6-terra|sol` **explicit pin only**.
`cursor/claude-opus-5` is step 1, not ladder-top. `seat_unsure ⇒ consult(independent_binder)`;
`binder_unsure ⇒ human`. CDP operator stuck ⇒ `cursor-auto` → nested `cursor-sdk`, never Cowork Ask.

## Hard walls

- `anthropic/*` **PROHIBITED**; `cursor/claude-fable-5{,-1}` blocked — Fable is `cdp/fable` only.
- Non-primary `cursor/*` bind ⇒ **operator-gated** unless a standing rule/skill names the default.
- `judgment_closed ∧ mechanical_remainder ⇒ split`: bind (`none`) → nested `cursor-sdk` implement (`nest_under` lease holder).
- Before **any** `team_dispatch`: consult-routing gate; `busy_status` shows `agent_bus`+`stargate`+`git_integration_worker` up.
- Same fix failed twice ⇒ stop; consult — never a third variant.
- `operator_names(param) ∧ ¬admits(param) ⇒ report(failure) ∧ wait`.

## Runbook binding

**Invariant:** `cite(runbook:<id>) ⇒ load(cited_section*) ∧ execute(seat-bound_steps)` before turn end.
Partial cite ⇒ that section only. Skills/rules say *when*; runbook owns *how*. Cite-from-memory = arm-without-relay.

## Front door

```
∀ operator_request:
  explicit override (/path-sim · /layer · root id · belt) ≺ tree
  mutates_durable_state ⇒ conductor session, bound to exactly one todo: work item
  else (answer · review · judgment, attended) ⇒ in-seat, threadless — reasoning-posture aperture
```

B legs spawn T0 conductor; materializer refuses non-todo `source_ref`. CHECKPOINT = reconstitution index only.
`seat=cursor-auto ⇒ silent SOP`; true operator-only ⇒ `status:needs-attended` + one recommended answer.
Mutating Task prompts ⇒ inject kernel (`task-subagent-parity_ulg`). Ladder detail: Use the `consult-routing` skill.
Doctrine: `decision:operator-request-front-door` · `decision:gate-independence-not-human-ness` ·
`decision:cursor-non-primary-model-operator-gate` · `anthropic-dispatch-authorization_ws.mdc`.
