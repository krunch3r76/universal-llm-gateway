---
description: Per-model frontier dispatch descriptors (CapabilityDispatch) — shared rule; load docs/agent-guides/rules/capability-dispatch.md for substance
alwaysApply: false
rule_category: domain-ulg
apply_tier: catalog
trigger_match_terms:
  - capability
  - dispatch
  - frontier
  - knob
  - descriptor
---
# Capability Dispatch — Cursor stub

**Shared rule (SOT):** `docs/agent-guides/rules/capability-dispatch.md`

Co-maintained by **cursor** and **web-claude**. Edit body in
`agent-surface/sources/capability-dispatch.md`, then:

```bash
scripts/gen-rules --target agent-guides-rules
```

CI drift gate: `scripts/agent-surface-check` (agent-guides-rules target).

## Invariant (one line)

∀ cloud model id (`provider/model`): the descriptor is **`CapabilityDispatch`**
(dispatch facet) — **¬** `ModelWrapper` (wrapper = translation mechanism only).

## Load the shared rule when ANY of:

- Adding or changing a cloud/frontier model in `capability_dispatch/registry.py`
- Debugging `reasoning_effort`, `max_tokens`, or frontier knob rejection (G9/G13)
- Authoring specs/handoffs that reference model generation knobs
- Briefing another agent on `openai/gpt-5.5` (or any `provider/model`) dispatch

```
fs(sandbox="workspaces", op="md_read",
   path="universal-llm-gateway/docs/agent-guides/rules/capability-dispatch.md",
   section="Agent interface — lookup (read)")
```

## Cursor-only

- Rule picker / `@capability-dispatch_ws.mdc` — this stub; substance is the shared file above
- Index entry: `.cursor/rules/index.mdc` § On-Demand — Frontier / cloud model work
