# Resume fill sources — load when speaking the resume

Slot **order** = `operator-posture` Rule 3. This file owns **fill sources**. Slot 6 is the only slot `continuity-thread-shaping` originates — spoken form of "the imprint is already on the graph."

| Slot | Filled from | ¬ |
|---|---|---|
| 1 Mission | card `## Objective` · hub description | slug · seeded todo |
| 2 In / Out | same block | widen silently |
| 3 been / are | tip `## State` + Handoff pointer | `## Windows` · linear thread |
| 4 `In one line:` | tip Handoff *State (1 line)* | omit the label |
| 5 Settled / Live / Next | newest sit-journal **sidecar** · tip `## Next pickup` | snapshot tables on the card · inventory dump · scoreboard paste |
| 5b Lane rules | card `## Rules` + named `runbook:*` bodies | omit and follow a global skill omit-path |
| 6 Tab + reconstitution | tip CHECKPOINT turn# · leftover imprint assertion/edge ids on hub or matter · resume CITE turn | chat as memory |
| 7 What I need from you | `OPERATOR_GATE` / `HOLD_MERGE` · hub deadlines | model-seat work · direction quiz |
