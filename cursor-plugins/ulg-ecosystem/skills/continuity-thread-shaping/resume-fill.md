# Resume fill sources — load when speaking the resume

Slot **order** = `operator-posture` Rule 3. This file owns **fill sources**. The imprint line is the only slot `continuity-thread-shaping` originates — spoken form of "the imprint is already on the graph." It follows the picture. It does not open it.

| Slot | Filled from | ¬ |
|---|---|---|
| 1 Aim | card `## Why this house`, plus the defect the tip established | next-pickup · a path · "follow the reply" · slug · seeded todo |
| 2 In / Out | card `## Objective` | a hop written as the objective · widen silently |
| 3 Where we left off | `tape_tail` — last two speech rows of `tape_verbal`, a sentence or two each, in the words that were said. Empty pour: `tape_left_off_gap` when set ("the last few turns aren't sealed yet, so I'm going from the checkpoint"); otherwise say the pour has no recent exchange and you are going from the checkpoint | skip the beat · a paraphrase that is not the chat · the receipts line standing in for the quote |
| 4 Already | what the tip says landed, and the gap that left the house open | a passing test spoken as the live check · `## Windows` · linear thread · repeating this as Been |
| 5 Object | `checkpoint_object`, else tip residue `Object:` — one noun phrase, then the order of moves | the hop itself · an object that lives only on another house |
| 6 Are / Going | tip state; Going = the current move only (`Next`) | Been · later moves inside Going · scoreboard paste |
| 7 `In one line:` | the aim again — outcome and constraint together | the hop · omit the label |
| 7b Lane rules | card `## Rules` + named `runbook:*` bodies (walk the pointed todo; ¬ treat a status sentence on the runbook as SOT) | omit and follow a global skill omit-path |
| 8 Receipts | one skippable line after the picture: tip CHECKPOINT turn# · `seal_status` · `summary_row_source` · leftover imprint assertion/edge ids. Label it a receipt. This is not the proof the tape was read | these facts leading the reply · chat as memory |
| 9 What I need from you | `OPERATOR_GATE` / `HOLD_MERGE` · hub deadlines | model-seat work · direction quiz |
