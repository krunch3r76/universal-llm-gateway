Orchestrate a durable continuity root or formal mission lane.

This command wraps skills. Use `orchestration-lanes` for session conversion,
root/mission birth, and clean-context resume. Use `mission-operator` to run a
formal mission and keep its turn schema conformant. Use
`checkpoint-discipline` for root CHECKPOINT maintenance.

For session-to-root conversion, follow the existing birth gate:
mint the continuity charter surface, post the birth CHECKPOINT with objective
and concrete `Next-pickup`, then stamp `role:root`. Session close still records
provenance; the root carries continuity.
