---
name: grokbuild-dispatch
description: RETIRED — use cursorbuild (code dispatch) or frontier_dispatch (Grok consult). Historical playbook at agent-skills/grokbuild.md (cortex).
---

# RETIRED

grokbuild harness archived (11588). **cursorbuild** / Cursor in-IDE for code; **frontier_dispatch** (`xai/grok-4.3`) for Grok consult. Do not call `grokbuild(...)`. History: `fs(sandbox="cortex", op="read", path="agent-skills/grokbuild.md")`.
