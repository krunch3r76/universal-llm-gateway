---
name: lead-agent-git-integration
description: "RETIRED — git_* MCP discipline folded into git-posture. Do not load; use git-posture for headless git_commit / gated merge primitives."
lifecycle: retired
---

# Lead-Agent Git Integration — RETIRED

**Status:** `lifecycle=retired` (2026-07-15).

Superseded by Use the `git-posture` skill — Lane A `git_commit` recipe and headless `git_*` routing live there. MCP `skill_uri` bindings retargeted to `rule:git-posture`.

Historical authority (unchanged): `decision:lead-agent-git-integration`.

¬ discoverable via `/boot-skills` / skill body API. Maintenance: `include_non_active=true` or this file under `.cursor/skills/lead-agent-git-integration/`.
