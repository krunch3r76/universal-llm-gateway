Analyze the current code and the most recent applied edits.
Identify any changes to the externally exposed HTTP API surface.

Specifically:
1. List any new, removed, or renamed endpoints (paths and methods).
2. Note parameter, query, or body schema changes.
3. Highlight altered response formats, status codes, or headers.
4. Identify breaking changes that would affect existing API clients.
5. Suggest updated examples for each changed route (request + response).

Return a concise changelog titled “HTTP API Surface Changes” with sections for:
- Added
- Modified
- Removed
- Potentially Breaking
