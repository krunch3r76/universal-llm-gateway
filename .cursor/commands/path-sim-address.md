**Retired.** `/path-sim-address` is a redirect to **`/path-sim`** (bundled mode).

Do: load [`.cursor/commands/path-sim.md`](path-sim.md) and run its When → lead obligations. Machinery: Use the `path-sim` skill § Bundled dispatch.

```
/path-sim todo:{slug}
/path-sim a:{assertion_id}
/path-sim friction {assertion_id}
```
