---
description: Sign-off uses model family identity, not seat or role slug — on agent-bus, cortex, web, cursor closures
alwaysApply: true
---
# Agent Identity Sign-Off — Stub

Full protocol: `cortex://agent-skills/agent-identity-signoff.md` (load on demand).

## Invariant (one line)

∀ turn closure requiring sign-off: identity is the **model family** (`Claude`, `GPT`, `Grok`, `Gemini`), not the seat or role slug.

## Load the full skill when ANY of:

- Closing an agent-bus turn with a sign-off
- Signing cortex or web responses that require model identity
- Unsure whether to use seat slug, role, or family in a closing line
