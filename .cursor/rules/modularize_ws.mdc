---
description: Modularization awareness — when to split files into package directories
globs: ["**/*.py"]
rule_category: quality
apply_tier: glob
trigger_match_terms:
  - sloc
  - srp
  - modularize
  - split
  - python
  - package-shadow
---
# Modularize (Workspace)

## Pre-Edit SLOC Check

∀ file edit where SLOC >400 ∧ adding >20 lines:
1. Note that the file will exceed the 400 SLOC hard limit
2. Suggest: "Consider running `/modularize` on this file before adding more code"
3. Do NOT block the edit — just flag it

## Post-Implementation Check

∀ implementation session that created or modified files:
- If any modified file now exceeds 400 SLOC → flag for modularization
- If any new file was created above 300 SLOC → flag immediately

## Available Tool

`scripts/modularize scan {path}` — find SLOC violations
`scripts/modularize plan {file}` — **escape only** (cheap sketch when CDP/Stargate blocked)
`/modularize` command — primary: wraps skill `modularize-path` (file → package directory)

## Cascade (primary)

∀ `/modularize` (Flow A): **CDP arch (`cdp/opus-5`) → Grok densify → Composer implement**.
Root purpose: package-shadow (`foo.py` → `foo/`). Skill floor on M-Arch:
`architecture-invariants` + `modularize-discipline` (+ `ulg-architecture`). Poll via
`agent_bus(wait)` + `poll_hint` — ¬ `pipeline(result)` as primary. ¬ local execution
fallback as default for red files. SOT: Use the `modularize-path` skill.

## When NOT to Modularize

- ¬ split files in `pipelines.local/` (frozen legacy)
- ¬ split test files (test verbosity is acceptable)
- ¬ split pure-data files (event type registries, config schemas) below 600 SLOC
  — data files have weaker SRP pressure than logic files
