---
description: Git revert scope constraint — no broad revert on live checkout. Forbidden broad forms (git checkout -- . / git reset --hard / git restore .). Permitted only with explicit file paths. Arc worktree exception.
alwaysApply: true
---
# Git Revert Scope (Workspace)

Headless / arc-seat git-CLI guidance; in a Cursor IDE session use editor undo / revert UI instead (`commit-and-git-scope_ws.mdc` Inv. 2). The forbidden forms below bind **every** seat.

**Invariant**: ∀ git-revert operation: scope MUST be explicit. The live checkout is shared — a broad revert silently wipes in-progress edits, staged hunks, and other agents' unrelated diffs.

**Forbidden** (broad — wipes the shared tree): `git checkout -- .` · `git restore .` · `git reset --hard` (any form, incl. `HEAD`; unconditional in the live checkout) · any glob/recursive form touching files beyond the agent's own diff.

**Permitted** (scoped): `git checkout -- <file>` (one explicit path per call) · `git restore <file>` · `git checkout -- <dir>/` only when the agent exclusively owns that subtree in an arc worktree.

**Arc worktree exception**: in a dedicated arc worktree (`…/ulg-grok-worktrees/<name>/`) broad revert is permissible (single-owner); NOT in the live checkout (`…/universal-llm-gateway/`).

Before any scoped revert: list the exact authored files, one targeted call each (no glob/`.`), then verify `git status --short`.
