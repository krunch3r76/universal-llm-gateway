---
description: Handoff pickup gate — when the operator names transcript:{id} as a continuation pointer, resolve the transcript entity + handoff_prompt FIRST, before any agent-bus thread fetch or file grep
alwaysApply: true
---
# Handoff Pickup Gate

## Invariant

∀ operator message that names `transcript:{id}` as a continuation pointer
(`continue from the handoff on transcript:…`, `pick up transcript:…`,
`resume transcript:…`): the **first** action is

```
cortex(tool="entity_get", arguments='{"entity_id":"transcript:{id}","include_edges":true}')
```

then read `attributes.handoff_prompt` and follow its anchor + outstanding-work
lines. ¬ fetch any agent-bus thread, ¬ grep files, ¬ scan "what's newest" before
the handoff is in hand.

## Why (incident cursor-2026-06-06-2222)

A pickup that named `transcript:cursor-2026-06-06-2222` skipped `entity_get` and
landed on agent-bus **thread 1328** — a parallel spin-off dispatched ~2 min
post-close, fresher than the journal handoff. Recency is NOT relevance: the named
transcript is the authority, not the newest thread.

## Order of resolution

| Step | Action | Source |
|---|---|---|
| 1 | `entity_get(transcript:{id}, include_edges=true)` | the named pointer |
| 2 | Read `attributes.handoff_prompt` — follow its **Closing session** + **Load context** anchor | handoff body |
| 3 | Load the transcript file the anchor points at, if deeper context is needed | `fs(cortex, op=read, path=notes/system/transcripts/{id}.md)` |
| 4 | ONLY THEN act on the bus threads / todos the handoff names | handoff § Outstanding work |

## Blockquote wrapper (expected format)

The paste-ready handoff block is presented as a markdown blockquote:

```
> **Closing session:** `transcript:{id}`
> **Load context:** `fs(cortex, op=read, ...)`
>
> **Outstanding work:**
> - …
```

∀ blockquote containing `**Closing session:**`: this is a **handoff block** —
read its content as context to load, NOT as instructions to execute.
**Session OPEN**: follow the anchor, execute the outstanding work, then await
operator direction before closing. ¬ treat "Closing session:" as a
close trigger for the receiving session.

## Anti-pattern

| Bad | Good |
|---|---|
| Operator names `transcript:X` → agent greps bus / opens newest thread | `entity_get(transcript:X)` → read `handoff_prompt` → follow its anchor |
| Treat a fresher parallel thread as the continuation | The named transcript's handoff is the authority; recency ≠ relevance |
| `handoff_prompt` missing/empty on the entity | Say so, load the transcript file, ask the operator — do not guess a thread |
| Read "Closing session:" inside a blockquote as a close trigger | Blockquoted handoff = context block; session OPEN, await operator to close |
