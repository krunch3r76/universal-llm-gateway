---
description: Cortex feature registry — classification invariant for foundation vs extensions
globs: ["libs/cortex_store/**/*.py", "services/mcp-server/tools/cortex*.py", "pipelines/dream_state/**"]
alwaysApply: false
---

# Cortex Feature Registry (Workspace)

## Invariant

**Invariant**: ∀ Cortex features in `libs/cortex_store/`: classified in `docs/cursor/cortex-registry.md`

The registry maintains two views:
- **Foundation** — 11 Cortex foundation primitives with AGM coverage where applicable
- **Extensions** — lived capabilities beyond the foundation (pre-v3, phase A–D, post-v3)

## When Adding or Modifying Cortex Features

1. Check `docs/cursor/cortex-registry.md` for existing classification
2. **New feature**: add a row with origin, classification, implementation path, endpoints, MCP op
3. **Foundation change**: name the Cortex foundation primitive being extended and cite the relevant AGM postulate when the change affects revision semantics
4. **Extension**: document origin (pre-v3 / phase A–D / post-v3) and classification (retrieval / operational / coordination / infrastructure)
5. Update the registry in the same commit as the feature

## Classification Types

| Type | Scope |
|---|---|
| `retrieval` | Affects how information is found (search, activation, ranking, boot sections) |
| `operational` | Affects how information is managed (quality gates, enrichment, dedup, staging) |
| `coordination` | Affects multi-agent behavior (boot scoping, journaling, domain detection) |
| `infrastructure` | Affects system plumbing (storage, embeddings, migrations) |

## Anti-Patterns

| Bad | Good |
|---|---|
| New endpoint in `routes/` without registry entry | Add registry row in same commit |
| Extending a foundation primitive without naming the affected primitive | State which Cortex foundation primitive is changing and whether AGM semantics are affected |
| New MCP dispatch op without classification | Add to Extensions table with classification |

## Reference

- **Registry**: `docs/cursor/cortex-registry.md` (agent-facing, provenance-grouped)
- **Capability reference**: `notes/system/shared/operational-lessons.md` (agent-facing, purpose-grouped)
- **AGM compliance report**: `docs/agm-compliance-report.md` (25/25 postulate tests, canonical)
- **Implementation proof**: `notes/system/kumiho-implementation-proof.md` (historical correspondence only; not normative)
