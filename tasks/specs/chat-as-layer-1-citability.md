# Chat as layer-1 citability

## Succession structural fill (human continuity speech tape)

When a window is sealed by succession harvest (`closed_by=succession`), a later real
human `/session-end` on the same tab performs **structural fill only** (A-1): the
verbatim layer PREFIX-EXTENDS from live JSONL; `closed_by` flips to the closing
agent; `sealed_by` / `sealed_on` remain seal provenance. No
`session.handoff_would_change` 409 on that path.

Event: `cortex.session_close.succession_structural_filled`.
